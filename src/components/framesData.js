const totalFrames1 = 91;

const frames = [];

for (let i = 1; i <= totalFrames1; i++) {
  frames.push(`/images/1/ezgif-frame-${String(i).padStart(3, '0')}.jpg`);
}



export default frames;
